import React from 'react';
import './PageHeader.css';

/*
A PageHeader component returns a <header> with the page's <h1>
*/
const PageHeader = (props) => {
  return (
    <header>
      <h1>ELF Computing | Laptops</h1>
    </header>
  );
};

export default PageHeader;